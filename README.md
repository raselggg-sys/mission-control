# mission-control

Kanban-style mission control dashboard.

## Files
- `index.html` — single-page dashboard
- `data/` — board state

## Run
```bash
python3 -m http.server 8000
```
Open `http://localhost:8000`.
